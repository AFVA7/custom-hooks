# useForm
<hr />

Ejemplo:

```
      const initialForm = {
          name: '',
          age: 0,
          email: ''
      };
      
      const {formState, onInputChange, onResetForm} = useForm( initialForm );
```
